extends ../etendues/base.pug

block variables
    - var erreur = erreur;
    - var session = session;

block contenu
    section
        article.fiche
            h1 Étiquettes
            if chapitre.etiquettes.length > 0
                div.flexible.debut
                    each etiquette in chapitre.etiquettes 
                        form.barre(action=`/gestion/${chapitre._id}/etiquettes/suppression` method="post")
                            input(name="etiquette" type="text" value=`${etiquette}` readonly)
                            button(type="submit") Supprimer l'étiquette
            form.barre.mini(action=`/gestion/${chapitre._id}/etiquettes/ajout` method ="post")
                input.mini(name="etiquette" type="text" placeholder="Étiquette")
                button(type="submit") Ajouter l'étiquette
            a.bouton.attirant(href=`/gestion/${chapitre._id}`) Gestion